{
  "name": "simple-webwidget",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "generate:qr": "node scripts/generate-qr.mjs",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node --test",
    "check:production-security": "node scripts/check-production-security.mjs"
  },
  "dependencies": {
    "@aptabase/web": "^0.5.0",
    "@momentum-design/tokens": "^0.10.4"
  },
  "devDependencies": {
    "qrcode": "^1.5.4",
    "vite": "^8.1.5"
  }
}
